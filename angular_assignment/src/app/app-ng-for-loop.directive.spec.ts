import { AppNgForLoopDirective } from './app-ng-for-loop.directive';

describe('AppNgForLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNgForLoopDirective();
    expect(directive).toBeTruthy();
  });
});
